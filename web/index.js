async function run() {
    const abi = document.getElementById('abi').value;
    const ctrAdr = document.getElementById('ctrAdr').value;
    console.log(abi, ctrAdr);
    // console.log(await web3.eth.defaultAccount);
};
